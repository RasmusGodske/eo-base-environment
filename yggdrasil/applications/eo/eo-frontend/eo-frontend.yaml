eo-base-helm-chart:
  ingress:
    hosts:
      - {{ .Values.ingressDomain }}
    httpMiddelwares:
      - name: https-only
        namespace: default
{{- if .Values.applications.cert-manager }}
    tlsSecretName: elop-cert
{{- end }}
    pathExceptions:
      - /api
      - /auth
      - /admin
